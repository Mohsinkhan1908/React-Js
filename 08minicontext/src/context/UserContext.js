import React, { createContext } from 'react'

    const UserContext = createContext(); // global variable or Outer wrapper which provide context means inside elements can get its context  

export default UserContext;